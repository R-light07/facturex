// Este ficheiro não é estritamente necessário porque estamos a usar o pacote cors, mas pode ser usado para configurações personalizadas.

// Exemplo de configuração personalizada de CORS:
/*
const cors = require('cors');

const corsOptions = {
  origin: 'http://localhost:3000', // ou o URL do seu frontend
  optionsSuccessStatus: 200
};

module.exports = cors(corsOptions);
*/

// Por enquanto, vamos apenas usar o cors sem configurações, então não precisamos deste ficheiro.
// Mas deixamos aqui para referência.